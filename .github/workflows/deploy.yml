name: Deploy to Hostinger

on:
  push:
    branches: [production]  # No space after colon, no indentation for list items

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          composer install --no-dev --prefer-dist --optimize-autoloader

      - name: Upload files via SCP
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          key_path: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT || 22 }}
          source: "./*"
          target: "/home/u923189910/domains/happniess-perfume.com/public_html/system"
          strip_components: 1
          overwrite: true
          rm: false  # Explicitly set to not remove existing files

      - name: Run migrations and optimize
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          key_path: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT || 22 }}
          script: |
            cd /home/u923189910/domains/happniess-perfume.com/public_html/system
            php artisan down
            php artisan migrate
            php artisan optimize
            php artisan cache:clear
            php artisan view:clear
            php artisan route:clear
            php artisan config:cache
            php artisan up
