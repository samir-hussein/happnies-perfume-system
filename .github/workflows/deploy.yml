name: Deploy to Hostinger

on:
  push:
    branches: [production]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          composer install --no-dev --prefer-dist --optimize-autoloader

      - name: Setup SSH Environment
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HOSTINGER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.HOSTINGER_PORT || 22 }} ${{ secrets.HOSTINGER_HOST }} >> ~/.ssh/known_hosts

      - name: Create deployment archive (excluding .env)
        run: |
          tar --exclude='.env' \
              --exclude='.git' \
              --exclude='node_modules' \
              -czf /tmp/deploy.tar.gz .

      - name: Upload files via SCP (preserve existing)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT || 22 }}
          source: "/tmp/deploy.tar.gz"
          target: "/home/u923189910/domains/happniess-perfume.com/public_html/system"
          tar_tmp_path: "/tmp"
          tar_exec: "tar --skip-old-files -xzf"  # Skip existing files during extraction

      - name: Set correct permissions
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT || 22 }}
          script: |
            cd /home/u923189910/domains/happniess-perfume.com/public_html/system
            [ -f .env ] || cp .env.example .env  # Create .env if missing
            chmod -R 755 storage bootstrap/cache
            chmod 600 .env
            chown -R u923189910:o1007068740 .

      - name: Run migrations and optimize
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT || 22 }}
          script: |
            cd /home/u923189910/domains/happniess-perfume.com/public_html/system
            php artisan down
            php artisan migrate
            php artisan optimize
            php artisan cache:clear
            php artisan view:clear
            php artisan route:clear
            php artisan config:cache
            php artisan up
